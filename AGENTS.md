## Cursor Cloud specific instructions

### Repository State

This is a nearly empty repository containing only a `README.md` placeholder. There is no application code, no package manager configuration, no build system, and no services to run.

### Development Environment

- No dependency installation is required.
- No services need to be started.
- No lint, test, or build commands are available.

### Notes for Future Agents

- If application code is added in the future, this file should be updated with the relevant setup instructions, service descriptions, and testing guidance.
- Check the repository root for new `package.json`, `requirements.txt`, `pyproject.toml`, `Makefile`, `docker-compose.yml`, or similar files that may indicate new tooling or services have been introduced.
